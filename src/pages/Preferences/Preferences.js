import React from 'react';

class Preferences extends React.Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <div>This is for preferences</div>
        )}
}

export default Preferences;
